import { Container, Typography } from '@mui/material';

export default function StudentPanel() {
  return (
    <Container>
      <Typography variant="h4">Student Panel</Typography>
      <Typography>course enrollment ...etc go here.</Typography>
    </Container>
  );
}
