import { Container, Typography } from '@mui/material';

export default function AdminPanel() {
  return (
    <Container>
      <Typography variant="h4">Admin Panel</Typography>
      <Typography>User management and reports go here.</Typography>
    </Container>
  );
}
