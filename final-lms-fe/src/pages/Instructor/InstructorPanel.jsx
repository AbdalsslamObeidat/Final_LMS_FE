import { Container, Typography } from '@mui/material';

export default function InstructorPanel() {
  return (
    <Container>
      <Typography variant="h4">Instructor Panel</Typography>
      <Typography>Course creation and grading UI here.</Typography>
    </Container>
  );
}